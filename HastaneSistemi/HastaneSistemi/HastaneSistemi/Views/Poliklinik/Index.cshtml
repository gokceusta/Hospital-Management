@using HastaneSistemi.Models
@model List<Poliklinik>

<br />
<table class="table">
    <tr>
        <th></th>
        <th>Departmanlarımız</th>
        <th>Ayrıntılar</th>
        <th></th>
    </tr>

    @foreach (var x in Model)
    {
        <tr>
            <td></td>
            <td>@x.PoliklinikAd</td>
            <td><a href="#" class="btn btn-light">İncele</a> </td>
            <td></td>
        </tr>
    }
</table>